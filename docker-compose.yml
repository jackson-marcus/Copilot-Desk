services:
  api:
    build: .
    env_file: .env
    ports:
      - "8480:8000"
    volumes:
      - ./data:/app/data

  ui:
    build:
      context: .
      dockerfile: Dockerfile.ui
    environment:
      COPILOTDESK_API_URL: http://api:8000
    ports:
      - "8981:8501"
    depends_on:
      - api
